import React from 'react'

const Header = () => {
    const headerStyle = {
        color: '#eee',
        borderRadius: '10px',
      };

    return <h1 style={headerStyle}>Minhas Tarefas</h1>;
}
 
export default Header;